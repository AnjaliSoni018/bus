// import { Router } from 'express';
// import { getProfile, updateProfile } from '../controllers/user.controller';
// import { requireAuth } from '../middlewares/auth.middleware';

// const router = Router();

// router.get('/bus', requireAuth, getProfile);
// router.get('/bus/:id', requireAuth, getProfile);
// router.post('/bus', requireAuth, updateProfile);
// router.delete('/bus', requireAuth, getProfile);
// router.patch('/bus', requireAuth, updateProfile);

// export default router;
